[tool.tartufo]
cleanup = true
entropy = true
exclude-paths = ".tartufo-excludes"
exclude-signatures = []
json = false
regex = true
repo-path = "."

[tool.black]
exclude = '''
(
  /(
      \.eggs         # exclude a few common directories in the
    | \.git          # root of the project
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.venv
    | _build
    | buck-out
    | build
    | dist
  )/
  | config
  | common
)
'''
